<h1>Catálogo de animales</h1>

<div class="animals-body">
  <div class="panel panel-default">
    <div class="panel-body">
      <p class="lead"><%= @subclass.name.titleize %></p>
      <ul class="nav nav-pills nav-stacked">
        <% @subclass.animal_species.each do |specie| %>
          <li role="presentation"><%= link_to specie.name.titleize, specie_from_subclass_path(@subclass.animal_class.animal_division.name, @subclass.animal_class.name, @subclass.name, specie.name) %></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>

<p>Back to <%= link_to @subclass.animal_class.name.titleize, animal_class_path(@subclass.animal_class.animal_division.name, @subclass.animal_class.name) %></p>
